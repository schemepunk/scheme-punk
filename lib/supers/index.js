'use strict';

const destination = require('./destination');
const source = require('./source');
const transform = require('./transform');

module.exports = {
  destination,
  source,
  transform,
};
